package global;

class CurrentData {
    // Store all score
    public static var SCORE:Int = 0;

    // Collect all score, after done a wave, is will count back into SCORE
    public static var PRE_SCORE:Int = 0;
}